# ``Fixed_Primitives``

@Metadata {
    @DisplayName("Fixed Primitives")
    @TitleHeading("Swift Primitives")
}

The always-full column adapter: `Fixed<S>` wraps any `Store.Protocol & Buffer.Protocol` column and enforces the `count == capacity` invariant from construction onward.

## Topics
